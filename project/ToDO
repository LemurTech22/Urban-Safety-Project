Stack:
  - Storage      : Azure Data Lake Storage Gen2 (ADLS)
  - Ingestion    : Azure Data Factory (ADF) — parameterized pipelines
  - Warehouse    : Azure Synapse Analytics (serverless SQL pool)
  - Transforms   : dbt Core (local)
  - Quality      : dbt tests + ADF validation activity
  - Orchestration: ADF triggers (schedule + tumbling window)
  - Secrets      : Azure Key Vault
  - Monitoring   : Azure Monitor + Log Analytic
  
  
  [x] Complete Checks for api, and Cloud Connection.
  [x] Set up Azure subscription + resource group
  [x] Create ADLS Gen2 + bronze/silver/gold containers
  [X] Create Synapse workspace + serverless SQL pool
  [X] Write upload script (AzureBlobHandler OOP class)
  [X] Extract Urban Crash data → upload to bronze container
  [X] Inspect raw fields (5-row sample) → build Pandera schema
  [X] Build silver transform (clean categoricals, drop junk columns)
  [X] Build ADF parameterized ingestion pipeline
  [X] Build ADF quality gate pipeline
  [X] Set up ADF tumbling window trigger
  [X] Set up dbt Core connected to Synapse
  [X] Write dbt models: staging → intermediate → marts
  [X] Add dbt tests + documentation
  [X] Build gold analysis layer
  [X] Connect PowerBI to Gold Layer
  [X] Set up Azure Monitor alerts
  [X] Write README with pipeline DAG diagram
  [X] Fix Access Block in Folium Maps.


  1. Extract the Urban Crash data from the Socrata API
   → use your working extractor script (10-20k records)
   → upload raw data to bronze container

2. Build AzureBlobHandler class (OOP pattern)
   → mirrors your S3Handler concept
   → methods: upload_dataframe, list_files
   → import this into all Azure pipeline scripts


3. Schema validation with Pandera
   → inspect the raw API fields first (run a 5-row sample)
   → build Pandera schema for the crash dataset
   → run PII audit (lat/lon, location fields need checking)

4. Build silver layer transform
   → clean categoricals (Snow vs SNOW issue from your project notes)
   → standardize column names
   → drop irrelevant columns (vehicle ID, report number, metadata)
   → validate schema at bronze → silver transition

5. Build gold layer
   → crash hotspots by location bucket
   → severity distribution over time
   → weather + surface condition correlations
   → DUI / substance abuse frequency by area
=======================================================
COMPLETE
=======================================================
